//For publishing to Gradle Plugin Portal
apply plugin: "com.gradle.plugin-publish"

pluginBundle {
    website = 'https://github.com/linkedin/gradle-plugin-insight'
    vcsUrl = 'https://github.com/linkedin/gradle-plugin-insight'
    description = project.description
    tags = ['documentation', 'autogeneration', 'markdown']

    plugins {
        pluginInsight {
            id = "com.linkedin.plugin-insight"
            displayName = 'Gradle Plugin Insight'
        }
    }
}
